<%= turbo_frame_tag "location_#{@location.id}" do %>
 <%= render @location %>

<div>
  <%= button_to "Edit this location", edit_location_path(@location) %>

  <% if !@location.primary %>
    <%= button_to "Destroy this location", @location, method: :delete %>
  <% end %>
</div>
<% end %>